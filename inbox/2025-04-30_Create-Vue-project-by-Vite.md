---
date: 2025-04-30
type: fact
aliases:
  -
hubs:
  - "[[vue]]"
---

# Create Vue project by Vite

```sh
 npm create vite@latest

> npx
> create-vite

│
◇  Project name:
│  event-booking # Project name
│
◇  Select a framework:
│  Vue # Framework
│
◇  Select a variant:
│  Official Vue Starter ↗ # For Vue project, select this option for more detail selections

┌  Vue.js - The Progressive JavaScript Framework
│
◇  Select features to include in your project: (↑/↓ to navigate, space to select, a to toggle all, enter to confirm)
│  TypeScript, ESLint (error prevention), Prettier (code formatting) # select some features you want
│
◇  Install Oxlint for faster linting? (experimental)
│  No # No idea what it is (not included in the video), so I select No

Scaffolding project in /home/matt/Projects/event-booking...
│
└  Done. Now run:

   cd event-booking
   npm install
   npm run format
   npm run dev

| Optional: Initialize Git in your project directory with:
   
   git init && git add -A && git commit -m "initial commit"


 cd event-booking/
 npm install # Install dependencies
 npm run dev # Start the development server

```
